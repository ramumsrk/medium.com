_SQL_ Structured Query Language scripts in this folder or namespace must be executed before trying anything else in _postgresql_ branch

The _03\_DROP\_TABLESPACE\_medium\_dot\_com.sql_ must be executed _only_ if errors are encountered after executing _02\_CREATE\_TABLESPACE script